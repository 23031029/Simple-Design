<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Simple Design</title>
</head>
<body>
    <div class="container">
        <br>
        <div class="text-center"><h2>Update Quantity</h2></div>
        <br>
        <div class="row justify-content-center">
            <div class="col-md-6">
                <% if (cart) { %> 
                    <form action="/edit/<%= cart.orderId %>" method="POST">
                        <div class="mb-3">
                            <label for="quantity" class="form-label">Quantity:</label>
                            <input type="number" class="form-control" id="quantity" name="quantity" value="<%= cart.quantity %>">
                        </div>
                        <button type="submit" class="btn btn-primary">Update Quantity</button>
                    </form>
                <% } else { %>
                    <p class="text-center">No furniture found.</p>
                <% } %>
                <a href="/cart" class="btn btn-secondary mt-3">Back</a>
            </div>
        </div>
    </div>
</body>
</html>
