<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Design</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .view {
            position: fixed;
            bottom: 20px;
            right: 20px;
        }
        
    </style>
</head>
<body>
    <header>
        <%- include('partials/navbar.ejs') %>
    </header>
    <div class="container">
        <br>
        <div class="text-center"><h2>Product Details</h2></div>
        <br>
        <% if (furniture) { %>
            <div class="text-center">
                <img src="/images/<%= furniture.image %>" class="img-fluid" style="max-width: 70%;" alt="<%= furniture.name %>">
            </div>
            <div>
                <p><b>Name:</b> <%= furniture.name %></p>
                <p><b>Category:</b> <%= furniture.category %></p>
                <p><b>Description:</b><br> <%= furniture.description %></p>
                <p><b>Category:</b> <%= furniture.material %></p>
                <p><b>Price:</b> $<%= furniture.price.toFixed(2) %></p>
                <p><b>Available Quantity:</b> <%= furniture.quantity %></p>
            </div>
        <% } else { %>
            <p>No product found.</p>
        <% } %>
        
        <form action="/addcart/<%= furniture.furnitureId %>" method="post">
            <input type="hidden" id="furnitureId" name="furnitureId" value="<%= furniture.furnitureId %>">
            <input type="hidden" id="image" name="image" value="<%= furniture.image %>">
            <input type="hidden" id="name" name="name" value="<%= furniture.name %>">
            <input type="hidden" id="price" name="price" value="<%= furniture.price %>">
            <div class="mb-3">
                <label for="quantity" class="form-label">Quantity:</label>
                <input type="number" class="form-control" id="quantity" name="quantity" min="1" value="1">
            </div>
            <button type="submit" class="btn btn-primary">Add to Cart</button>
        </form>
        
        <div class="mt-3">
            <a href="/product" class="btn btn-secondary">Back to Products</a>
        </div>
    </div>

    <footer>
        <a class="view" href="/cart"><button class="btn btn-success">View Cart</button></a>
    </footer>
</body>
</html>


