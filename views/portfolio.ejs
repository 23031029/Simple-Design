<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Simple Design</title>
    <style>
         .card {
            margin: 20px;
            border: 1px;
            width: 70%;
            margin-left: 15%;
            background-color: #272727 !important;
            color: white !important;
        }

        .card-body {
            padding: 40px;
        }

        .card-title {
            font-size: 1.25rem;
            color: white !important;
        }

        .card-text, .text-body-secondary {
            color: white !important;
        }

        .card-img-bottom {
            width: 100%;
            height: auto;
        }

        .card-img {
            width: 50%;
            height: auto;
        }

        /* Additional specificity */
        .card .card-text small {
            color: white !important;
        }
    </style>
</head>
<body>
    <header>
        <%- include ('partials/navbar.ejs') %>
    </header>

    <% for(let i = 0; i < portfolio.length; i++) { %>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><%= portfolio[i].style %></h5>
                <p class="card-text"><b>Designer:</b> <%= portfolio[i].name %></p>
                <p class="card-text"><b>Design Type:</b> <%= portfolio[i].type %></p>
                <p class="card-text"><%= portfolio[i].address %></p>
                <p class="card-text"><small class="text-body-secondary"><b>Duration:</b> <%= portfolio[i].duration %></small></p>
                <p class="card-text"><small class="text-body-secondary"><b>Price:</b> $<%= portfolio[i].price %></small></p>
            </div>
            <img src="/images/<%= portfolio[i].image %>" class="card-img-bottom" alt="<%= portfolio[i].image %>">
        </div>
    <% } %>
    

</body>
</html>
